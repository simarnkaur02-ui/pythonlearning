a = {3, 23, 1}
b = {23, 4, 2, 55, 1,}
 

# c = a.union(b) # Contains all the element in a along with all the element in b
# print(c)

# d = a.intersection(b) # Contains only the elements that are present in a as wll as b
# print(d)

# e = a.difference(b)
# print(e)

# f = a.symmetric_difference(b) # Contains all the elements that are present in a or b but not in both
#print(f)
g = {1,2,3}
h = 4
g.clear()
print(h)  # Clears the set